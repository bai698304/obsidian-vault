# 最大似然估计法

## 基本思想

对未知参数 $\theta$ 进行估计时，在该参数可能的取值范围内选取使"样本获此观测值 $x_1, x_2, \cdots, x_n$"的**概率最大**的参数值 $\hat{\theta}$ 作为 $\theta$ 的估计。

即 $\hat{\theta}$ 最有利于 $x_1, x_2, \cdots, x_n$ 的出现。

## 步骤

### (1) 写似然函数

- **离散型总体**：$L(x_1, \cdots, x_n; \theta) = \prod_{i=1}^{n} p(x_i; \theta)$
- **连续型总体**：$L(x_1, \cdots, x_n; \theta) = \prod_{i=1}^{n} f(x_i; \theta)$

### (2) 求参数

- 取对数：$\ln L(\theta)$
- **若似然函数有驻点**：令 $\frac{dL}{d\theta} = 0$ 或 $\frac{d(\ln L)}{d\theta} = 0$，解出 $\hat{\theta}$
- **若似然函数无驻点（单调）**：用定义求 $\hat{\theta}$（取边界值使 $L$ 最大）
- **若似然函数为常数**：用定义求 $\hat{\theta}$，此时 $\hat{\theta}$ 不唯一

## 最大似然估计量的不变性原则

设 $\hat{\theta}$ 是 $\theta$ 的最大似然估计，函数 $u = u(\theta)$ 具有反函数，则 $\hat{u} = u(\hat{\theta})$ 是 $u(\theta)$ 的最大似然估计。

> 如 $\hat{\theta}$ 是 $\theta$ 的最大似然估计，则 $e^{\hat{\theta}}$ 是 $e^{\theta}$ 的最大似然估计。

## 相关概念

- [[参数的点估计]]
- [[矩估计法]]
- [[无偏性]]
