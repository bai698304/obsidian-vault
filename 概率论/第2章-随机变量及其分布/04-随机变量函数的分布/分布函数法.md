# 分布函数法（定义法）

## 方法

设 $X$ 为连续型随机变量，其概率密度为 $f_X(x)$，$Y = g(X)$，则直接由定义求 $Y$ 的分布函数：

$$
F_Y(y) = P\{Y \leqslant y\} = P\{g(X) \leqslant y\} = \int_{g(x) \leqslant y} f_X(x) \, dx
$$

如果 $F_Y(y)$ 连续，且除有限个点外 $F_Y'(y)$ 存在且连续，则 $Y$ 的概率密度 $f_Y(y) = F_Y'(y)$。

## 优点

- 最可靠，适用于任何情况
- 在 $Y = g(X)$ 是非单调函数时，一般比[[公式法（单调变换）|公式法]]方便
- 通过作图得出 $X$ 的取值范围

## 相关概念

- [[一维随机变量函数的分布]]
- [[公式法（单调变换）]]
- [[分布函数]]
