# 随机变量的相互独立性

## 定义

### 二维随机变量的独立性

设二维随机变量 $(X, Y)$ 的分布函数为 $F(x, y)$，边缘分布函数分别为 $F_X(x)$，$F_Y(y)$，如果对任意的实数 $x, y$，都有

$$
F(x, y) = F_X(x) \cdot F_Y(y)
$$

则称 $X$ 与 $Y$ **相互独立**，否则称 $X$ 与 $Y$ 不相互独立。

### $n$ 维随机变量的独立性

如果 $n$ 维随机变量 $(X_1, X_2, \cdots, X_n)$ 的分布函数等于边缘分布函数的乘积，即

$$
F(x_1, x_2, \dots, x_n) = F_1(x_1) \cdot F_2(x_2) \cdot \dots \cdot F_n(x_n)
$$

则称 $X_1, X_2, \cdots, X_n$ **相互独立**。

## 充要条件

### 离散型

$$
X \text{ 与 } Y \text{ 相互独立} \Leftrightarrow P\{X = x_i, Y = y_j\} = P\{X = x_i\} \cdot P\{Y = y_j\} \quad (\forall i, j)
$$

### 连续型

$$
X \text{ 与 } Y \text{ 相互独立} \Leftrightarrow f(x, y) = f_X(x) \cdot f_Y(y)
$$

## 性质

1. 若 $X_1, \cdots, X_n$ 相互独立，则其中任意 $k (2 \leqslant k \leqslant n)$ 个也相互独立
2. 独立时，条件分布等于边缘分布
3. 若 $X_1, \cdots, X_n$ 相互独立，$g_i$ 为一元连续函数，则 $g_1(X_1), \cdots, g_n(X_n)$ 也相互独立
4. 分组作函数运算后仍相互独立（变量不重叠）

## 相关概念

- [[事件的独立性（两个事件）]]
- [[独立性与不相关性的判定]]
- [[二维正态分布]]
